import React from 'react';
import { Box, Typography } from '@mui/material';
const EventBox = () => {
  return (
    <Box
      sx={{
        borderColor: 'red',
        width: '100vm',
        display: 'flex'
      }}
    >
      Hello World
    </Box>
  );
};

export default EventBox;
